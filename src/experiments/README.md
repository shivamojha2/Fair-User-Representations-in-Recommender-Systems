## Customize training parameters for running different experiments

`template.json` can be used to run various experiments. This template can be used for running MLP recommender system without filter.
